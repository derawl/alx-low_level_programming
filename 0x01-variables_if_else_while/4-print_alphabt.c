#include <stdlib.h>
#include <time.h>
/* more headers goes there */
#include <stdio.h>

/* betty style doc for function main goes there */
/**
 *  * main - Entry point
 *   *
 *    * Return: Always 0
 *     */

int main(void)
{
		char a;

			for (char b = 'a'; b <= 'z'; b++)
					{
								if(b != 'e' && b != 'q'){
											    putchar(b);
											    		}
									}

				putchar('\n');

					return (0);
}
